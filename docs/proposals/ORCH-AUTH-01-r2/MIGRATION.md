# Advisory authority migration sequence

TRANSITION-CONTRACT.md owns the mandatory safety outcomes; APPROVAL-SCOPE.md defines the approval digest. This document is an implementation plan.

At source e590e38, preserve the old authority base 0f4ab3e5ef39bda07d6e77356ad53fca9136cdd5, schedule anchor 345591818b220964618dc4e80cce3c0e0213783c and complete WP6e ledger. A schedule amendment, regenerated lock hash or one-shot commissioning cannot authorize this transition.

## Bounded increments

1. Qualify the existing WSL engine with the real OCI fixture. Establish suitable disposable host isolation, one successful public workflow plus its rejection, authenticated read-only evidence handoff and early native Windows feasibility. WSL supplies Linux evidence only.
2. Add compatible source/legacy readers, scoped evidence and registry support while the old epoch stays valid. Develop one real consumed distribution build and supporting checks, then expand coverage in separately verified increments.
3. After explicit normative-digest approval, commit exact inert old/new authority snapshots and the actual approval reference. The new anchor is a real future ancestor commit, never an invented hash.
4. Commit a separate descendant request binding the approved normative digest, exact snapshots/active hashes, old ledger tip and current audited implementation identity. Review its concrete rendered transition.
5. Apply from a clean attached clone using the existing lease and recoverable intent publication. Proposed wiring is pnpm loop:authority:migrate -- --request <committed-request>; no implementation is claimed.
6. Verify consumer agreement on the coherent generation and commit the complete activation. Preserve all original history. Missing qualification remains NOT_READY; old placeholder checks are not declared passed.
7. Run full fresh native-platform qualification and then the separate live/human gate. Fence old state and require independently reviewed reconciliation before adoption.

The first implementation increment proves feasibility; it does not build the whole coordinator. Candidate checks cannot replace exact closure required by integration or reconciliation.

## Approval and implementation changes

Activation binds the approved normative digest and exact authority bytes. Conforming advisory changes update the package integrity inventory and code/conformance review without renewed approval of unchanged authority. A normative or semantic contract/trust change requires a new approval target.

The lock's fixed snapshot prefix, strict ancestry, exact publication allowlist, independently validated approval, durable intent, mixed-generation refusal and no state adoption remain mandatory. Module organization and record implementation may evolve only within TRANSITION-CONTRACT.md.

WP6e stays blocked at its recorded identity. A future continuation needs explicit treatment of its outstanding gates and new provenance. This proposal does not supply that approval or alter WP6f.

