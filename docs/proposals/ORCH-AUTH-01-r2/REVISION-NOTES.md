# ORCH-AUTH-01 r2 — review refinements

Status: proposed, not approved or activated. The user agreed with the two review recommendations and noted that verification costs are already heavy. That is authority to revise this proposal, not approval of its frozen-authority payload.

The original r1 proposal and its audit remain untouched at ../ORCH-AUTH-01. Its content digest is 333bb70e0db13459304a9051fcbd862a567d7debf62bb6d6ebfc5c6c22a1011a. The complete original-file inventory is in revision-baseline.json.

## Changes

1. Candidate verification keeps the mandatory invariant/static/build/four-owner floor and explicit workflow checks for affected behavior. It does not automatically dispatch the full native Windows/Linux qualification matrix. Full qualification remains fresh and complete at the literal no-argument source gate and every operation that requires that gate.
2. The approval digest covers an explicit normative file set. Implementation plans, current command wiring, coordinator design notes, tooling and runtime assessments have a separate package integrity digest. A conforming advisory edit requires a new audit and package inventory, without requiring approval of unchanged authority.

All twelve final outcomes, eight domains, sixteen negative families and every variant on both native platforms, two independent clean builds per platform, exact toolchain/resource bounds, live human acceptance and historical mappings are preserved. Cadence is not a new verification profile, a scope-suppression graduation, cached closure evidence, or permission to integrate with candidate-only evidence.

The current protected five-job CI remains unchanged. This revision prevents the proposed additional qualification matrix from becoming a routine candidate dependency; it makes no measured speedup claim and does not remove existing verification costs.

## First implementation work after approval

Prove feasibility in small increments: qualify the existing WSL Docker engine with the real OCI fixture; establish an isolated qualification host; prove one real public workflow and one correctly reached rejection; test the authenticated producer/consumer evidence handoff; establish the native Windows provider route. Expand to the full matrix only after those foundations work. None of that implementation or runtime qualification is claimed by this document audit.

The first proposal remains a historical review target; r2 is the current recommended review target. Neither has received exact-digest authority approval. WP6e/WP6f retain their recorded status and provenance.

## Review evidence

The initial document seal passed thirteen consistency groups and five finite approval/package mutation cases. It verified all 26 protected source files and all 33 original proposal files unchanged. The new normative digest is 53d38a2c2038cd9c5ffc240275043709d23dd33a81237e3d12018a38a8378108; the current full package digest and validated receipt path are in proposal-audit.json. The execution plan records commands and the semantic diff. Review also made the proposed AGENTS prohibition on substituting no-op or unit-only results explicit. No product, runtime, migration or CI qualification is claimed.
