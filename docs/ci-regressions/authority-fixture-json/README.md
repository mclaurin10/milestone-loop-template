# Preserve legacy fixture boundaries under the strict authority reader

Status: fixture repair and retained audit pass. Clean post-commit reproduction and exact hosted rerun follow this cohesive commit. This supporting repair does not activate ORCH-AUTH-01 or complete WP6e.

The exact C6a commit `6ae4efb808652e24d565cf5562c0611aac5ed25a` failed both controller jobs in [run 34023606616](https://github.com/mclaurin10/milestone-loop-template/actions/runs/34023606616). Each raw report has 810 passed and the same 59 failed cases out of 869, across ten files. Protected lock/package fixtures contained plain path labels rather than JSON, so the new publication reader rejected them before their original recovery, reconciliation and inspection boundaries. Both adopter jobs and the OCI job passed. The later root-unit steps did not run in either failed controller job.

Ten fixture setup writes now provide a legacy schema-1 lock record, and the deterministic fixture also provides a JSON package object. Every remaining byte of those files is preserved, including original assertion bodies, fault loops and deadlines. The two external crash-worker source files receive the same narrow correction. Optional commissioning manifests remain absent where originally absent. Production readers, shared verification, authority files, state, ownership and workflows are unchanged.

All five provider ZIPs were rehashed and inspected before extraction. `audit-hosted.ts` independently validates their exact Git/runtime/job identities, 33 command-owned receipts and declared artifacts, both identical failure sets, the nine-stage completion-ineligible adopter aggregates and six real OCI outcomes. `hosted-audit-2` passes with receipt SHA256 `3865109d9b63ddf2fc4f9c9d1861b82e5f52afca7d01757a13ebf6012d7af07b`. The initial audit confused the command wrapper's ERROR with the raw test FAIL; that failed observer attempt remains retained and neither outcome was relabelled.

The representative native Windows reproduction failed four of eleven original cases. The complete native selection finished in 4162.91 seconds: 62/67 passed, with five original deadline failures and zero skipped cases or unhandled errors. Those failures are status/dry-run (15 seconds), read-only status (5), telemetry-finalization integration (60), review-rejected reconciliation (60), and the target-integration hard-loss matrix (600). No native PASS receipt exists. A subsequent one-case status diagnostic passed its selected case but is deliberately non-passing as a full-file check because eight cases were skipped. An unchanged pre-C6a `dba20a6` comparison executed the same five cases: both inspection cases passed, and the two 60-second cases plus the 600-second matrix timed out. The baseline also reported ENOTEMPTY cleanup and a 10-second cleanup-hook timeout; 34 cases were skipped and no PASS receipt exists. Its tracked files, private refs and source state are unchanged. These differing observations do not establish native qualification or a timing improvement. Typecheck, lint, format and five invariants pass; invariants took 132,262 ms. Original deadlines and timestamp checks are unchanged.

The complete actual `pnpm test:unit` development check failed in fresh VM run `17ba5952-3cb6-4742-b274-95fc602384e0`: 1042/1043 passed, including all 67 affected cases. The remaining original test-partitions omission-mutation case could not find its expected reduction artifact. No unit PASS receipt exists. The exact C3 host coordinator kept its original finite 30-minute policy and completed owned process/cgroup/directory cleanup; its overall status remains ERROR. The unchanged independent C3 OCI audit passes all six real cases in `vm-1/oci-audit-2`. A prior invocation supplied a matrix file instead of its directory and remains failed.

`prepare-vm.py` retains the precise additive guest program and input provenance. The projected tree is `c94eb34007a13d2d342f331fb1422b8325cc0141`, derived from real C6a plus the ten fixture edits; patch SHA256 `4401ea596c7e7fccada0f5511af7aaab231aa5cbb04f897e99d8c2a6c5f7fca8`. The returned failed-run archive was inspected across 489 members with SHA256 `63980f3d64e8aed92f5523c94422d3359090599ee2c80fb6c653359617ce20ea`. This is Linux development evidence, separately scoped from native Windows, a committed candidate or readiness. The final retained audit includes both VM outcomes and the diagnostic limitations below.

Supporting Linux diagnostics 1–6 preserve the unsuccessful outer pnpm invocation, the sanitized child's early preflight failures, an ineffective `.npmrc` configuration attempt and an initially unsupported strict-diagnostic option spelling. The accepted strict spelling exposes `ERR_PNPM_VERIFY_DEPS_BEFORE_RUN: The value of the enableGlobalVirtualStore setting has changed`. The unchanged production child environment removes `CI`; pnpm's default depends on that value. An explicit `enableGlobalVirtualStore: false` overlay in the isolated diagnostic checkout then lets the actual omission command reach the original semantic rejection with its reduction and no PASS receipt. The overlay is disclosed and supplies no exact candidate claim.

VM-2 run `a0655068-5890-48c1-a8af-65b4b68fb43a` completed with the same source projection and unchanged finite C3 host coordinator. Its retained pre-configuration omission command directly reproduced the noninteractive pnpm preflight ERROR with no reduction or PASS receipt. The additive guest procedure then created only `/home/qualifier/.config/pnpm/config.yaml` within that disposable account. The file contains only `enableGlobalVirtualStore: false`; production filters, repository source, dependency checks and test deadlines are unchanged. Actual `pnpm test:unit` passed 1043/1043 in 94 files, including the unchanged omission case, in 511107 ms; receipt SHA256 `2b2fa086a77948641b54742049f559553c806527f0031f0291e74ccd7693cd3f`. VM-1 remains ERROR.

The complete VM-2 lifecycle took 987288 ms and observed owned process/cgroup/directory cleanup. Its returned archive was inspected across 177 members, SHA256 `a67d08943e172fcfa284bd24400cd535561739886357e5827dafc805d2dccef8`. The unchanged independent OCI auditor passes all six cases, and the host auditor passes this one Linux lifecycle. Their receipt hashes are `3f4b683dc712773610ee8ec05c1452b61154e7051be9b019ae05da4ea20f310b` and `2f4a44fdaf6e93fd8fb3053004d72e4fc1dcf5ee0173795ea84b53068b973c9f`. No native Windows, full source qualification or timing improvement is inferred.

`curate.ps1` retains bounded regular observations and exact tested inputs, excluding the 41 MB Git transport bundle and temporary index. `audit.ts` checks every retained hash and receipt, reconstructs the actual Git projection, proves fixture-only edits by whole-file comparison with the real baseline, and reconciles all original controller/affected case identities with the complete unit report. A fresh post-commit reproduction and all five exact hosted jobs follow the verified repair commit.

The seal contains 1347 regular files / 6491526 bytes and 49 independently validated receipts. ZIP SHA256 is `15d0ff6141b51179e31e0cb792f31d85999842dbc8d6e681a8f6044e0e95d89b`; the passing retained audit receipt is `0f86dcd448a20d36e02820bc95bff18e01c3ad782c73452a54789fc9fe79d89e`. The first auditor's failed uniqueness assumption is retained with its original code and input manifest. Three unchanged host-discovery parameter rows have the same Linux display name: both unit reports preserve all 1043 observations and the same 1041-name multiset. This audit makes no candidate exactly-once claim. The existing production comparator still refuses repeated names; the continuation must resolve that recorded gap without weakening its rejection.

After the evidence seal is present, reproduce its retained audit from this exact repair commit with the pinned runtime. The two output directories must be absent. The extractor verifies the ZIP digest and every entry before writing; the auditor does not execute archived scripts or dispatch a VM/controller.

```powershell
$env:PATH=(Resolve-Path '.tools/node-v24.18.0-win-x64').Path+';'+$env:PATH
$fixtureSeal=Get-Content -Raw 'docs/ci-regressions/authority-fixture-json/evidence/manifest.json' | ConvertFrom-Json
& docs/ci-regressions/wp6e-inventory/extract.ps1 -Archive 'docs/ci-regressions/authority-fixture-json/evidence/retained-evidence.zip' -ExpectedSha256 $fixtureSeal.archive.sha256 -Destination (Join-Path (Get-Location).Path 'artifacts/fixture-reaudit-input')
pnpm exec tsx docs/ci-regressions/authority-fixture-json/audit.ts artifacts/fixture-reaudit-input artifacts/fixture-reaudit-output
```

C6b is preserved in scoped stash `88f748f1a26dfa01d347ee9fd13652222abbb9a3`; its thirteen file hashes are recorded under `artifacts/wp6e-source-scopes-20260906/paused-c6b`. Restore them after this repair and continue complete source consumers, approved migration/request/review/publication, actual candidate/four partitions/65-minute boundary and committed 5(a)/5(b). Keep historical WP6e BLOCKED at `e590e38c32de2b5baa7423f66bbd8a0230b61839`, the untracked roadmap outside commits, source controller state absent and separate readiness/human gates incomplete.
