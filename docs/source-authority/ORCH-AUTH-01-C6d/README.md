# Exact transition projection and committed request binding

This C6d increment is supporting implementation evidence under approved ORCH-AUTH-01 r2. Legacy authority remains active. It authorizes no publication, readiness or state adoption. The actual migration request, independent review, complete generation reader, leased publication and WP6e continuation candidate remain separate work.

The production read-only CLI projects exactly twelve files from real clean implementation commit ab19211e3663acc7b14acc847bdc3dde11414d19 and strict ancestor snapshot 91cbd3eb75ec771cfa1f315fe2641488e361c9e0. It preserves the complete prior amendment ledger and fixed old authority. Its canonical subject digest is 23db42da61fb793061d12489cad38d915002f567c03abde656e321bb174bfceb. Thirteen command artifacts contain the report and twelve proposed files. These files are written only under the fresh artifact output.

The committed-request inspector requires the fixed request path, real implementation objects, strict snapshot ancestry, a separate direct-child request commit, an exact fixed evidence pin and byte-for-byte reconstruction of every output. Referenced evidence content is not authenticated as a successful implementation audit or independent review by this inspector. All such authority flags remain false. Normal request review inspection requires a clean legacy generation; pending publication refuses it before invocation. The metadata reader is not a recovery admission capability.

The full request suite passed 31 native cases. The final six-file boundary suite passed 132 cases, including the real distribution inventory and original adopter assertions. Three new reserved-source schedule cases first failed at generic fallback and now reject pending implementation of the complete source-generation reader. Typecheck, lint, formatting, source architecture and the five invariants have command-owned evidence in the seal. Failed attempts remain in the inventory.

One original adopter prose scan matched the substring d31 inside actual generated Git base 047cee6f2f26bf7c0ad63b591afd3175e9904162. Retain the failure and original test bytes. The correction checks that one opaque value against actual Git HEAD^ and the producer's recorded authority base, then applies the unchanged source-identity prohibition to all remaining text. Production generation, other assertions and deadlines are unchanged. A later 86-case raw-passing run was correctly denied a receipt because the requested file list included a nonexistent path; the final run uses the actual tools/source-release.test.mjs file.

The seal also contains clean C6c post-commit audit/dependency/consumed-build observations and the completed C6b e072287 hosted cohort (run 34033743564). All five exact jobs succeeded; each controller ran 915 controller and 1089 root-unit cases. The independent hosted audit validates five provider ZIP hashes and 43 child receipts. Root-unit success does not establish the historical unit-domain criterion.

Run the retained audit with Node 24.18.0 and pnpm 11.15.1 from this increment's clean commit. The extractor checks every ZIP member and digest before writing a fresh owned directory. The audit verifies the exact inventory, source/authority pins, actual Git prior states, raw reports and every command receipt/artifact; it does not rerun the tests or hosted jobs.

```powershell
$env:PATH=(Resolve-Path '.tools/node-v24.18.0-win-x64').Path+';'+$env:PATH
$taskManifest=Get-Content -LiteralPath 'docs/source-authority/ORCH-AUTH-01-C6d/evidence/manifest.json' -Raw | ConvertFrom-Json
& 'docs/ci-regressions/wp6e-inventory/extract.ps1' -Archive 'docs/source-authority/ORCH-AUTH-01-C6d/evidence/retained-evidence.zip' -ExpectedSha256 $taskManifest.archive.sha256 -Destination (Join-Path (Get-Location).Path 'artifacts/c6d-fresh-retained-input')
pnpm --config.verify-deps-before-run=error exec tsx docs/source-authority/ORCH-AUTH-01-C6d/audit.ts artifacts/c6d-fresh-retained-input artifacts/c6d-fresh-retained-audit
```

Historical WP6e remains BLOCKED at e590e38c32de2b5baa7423f66bbd8a0230b61839. No actual source candidate, 65-minute provider qualification, committed 5(a)/5(b), full native workflow qualification, human acceptance or WP6f interpretation is claimed by this increment.
