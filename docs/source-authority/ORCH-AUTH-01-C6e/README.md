# Recoverable source authority publication

C6e implements the compatible readers, actual implementation-audit producer, independent SDK review and leased publication required by approved ORCH-AUTH-01 r2. The normal consumer is `pnpm loop:authority:migrate`. Legacy authority remains active until a separately committed request, successful fresh review, coherent publication and explicit activation commit complete. This implementation increment grants no controller-state initialization or adoption.

The fixed audit executes all eight source prerequisite commands, validates their command-owned receipts and retains raw execution, report and consumed-build artifacts. The request binds a clean implementation commit and the strict ancestor snapshot. Review uses the fixed independent SDK gateway and structured acceptance checks; its in-process private permit is required by the publisher. Synthetic test review inputs cannot authorize a real migration.

Publication acquires the existing mutation lease and persists an exclusive fsynced intent before replacing the twelve exact outputs. Every boundary rechecks the lease, branch, index, request, intent and prior/new bytes. Recovery retains the first prepared intent and the original export packet while preserving each fresh review and lease owner. An observed durable intent is recorded as an observation when its creation event was interrupted. Foreign bytes are preserved and diagnosed. All consumers reject pending, mixed or rolled-back generations. Committed publication fences legacy state operations; it does not create replacement state.

The complete nine-file native regression run passed 203 cases, including every prior 107 focused identity, original crash/recovery cases, pre-intent retries, the intent-write gap, export interruption and committed native/tier consumers. The exact 650 working input files were audited before the freeze ended. The retained reconstruction checks 649 included files against actual Git objects, thirteen explicit raw CRLF/Git LF relationships and the real isolated precommit bundle. The excluded user roadmap is retained only as a hash.

The isolated verification input is commit `082e4369a8e637243ff45e9e66d368452c74eced`, tree `e45498ae34c9aa23ed17aceeea6f222fb854f518`, direct child of `fc2419d8bdfc3658fe76edf2b543b38051b359f1`. Its 331 source/test/config file pins match the frozen regression inputs. This is a verification input identity, separate from the eventual primary C6e commit.

Three precommit guests passed four static commands and failed the real dependency check. The first rejected relocated store metadata; the second rejected a skipped allowed build script; the third exposed that pnpm 11.15.1's store status compares installed files against the unbuilt index. All failed aggregates, their completed six-case OCI audits and independent cleanup audits remain retained. The later commands did not run.

The general repair checks mandatory store integrity against a fresh frozen offline unbuilt reference, then independently compares the working dependencies with a normal frozen offline built reference. Dependency report v2 binds both paths, command cwd, argv and exact raw log hashes. The original v1 decoder is restricted to the immutable prior inspector hash. There is no package-specific exception or ignored store failure. The real Linux command passes intact dependencies, rejects a deliberately modified installed package at the byte-comparison boundary, and restores the original bytes. Its independent audit checks 192 graph entries, including 138 installed packages.

The complete revised selection passed 260 tests across ten files: all prior 203 identities, eleven added evidence cases and all 46 unchanged source-release tests. Its independent audit rechecks all 331 executing source pins before and after, raw identities, actual dependency/corruption evidence and four static receipts. These working-tree observations have their own Linux input identity. The exact four-file delta is separately retained and committed atop the original isolated input as `1c386ec95b628a2323f8d5dab351ad72a59abe3a`, tree `4e4d2ae0eae69784263f6d18668c5bb070f94c62`. Both real incremental bundles preserve their own trees and parent relationships.

The fourth disposable guest timed out at the unchanged C3 30-minute lifecycle while the controller suite was active. No guest archive survived; its collector failed on the missing archive, while independent cleanup passed. Its earlier command-completion events cannot replace missing child receipts. A separate fresh same-input C3 lifecycle subsequently passed all six actual OCI cases and independent host/OCI/cleanup audits without appending the long source suites. The separate Linux source run at `1c386ec` passed its first six commands but failed the complete controller suite: 1024 passed, 42 failed, no pending/todo. Root unit and build did not run. Two earlier failed namespace setups also remain retained: a denied interface query before dispatch and an incorrectly detached checkout. No failed run supplies a source aggregate PASS.

That broad failure exposed an incompatible rollback-history guard for genuine unborn legacy Git repositories and a task-owned cache configuration gap under the unchanged sanitized environment. The repaired guard inspects all refs and reflogs, explicitly requires HEAD to resolve as a commit when present, and rejects command errors or type diagnostics. The original state tests then exposed repeated Git startup cost at their unchanged deadlines. A typed, length-checked `cat-file --batch` response now combines the private commit type/content reads while preserving every authority, ref, mirror and hook fence. Original test bodies and all prior identities remain unchanged. Six native repair attempts, including the earlier non-commit HEAD failure and original deadline failures, retain their exact inputs; the final six-file run passed 130 cases.

The unchanged package-runtime test separately passed inside the task-owned private namespace, with its UID home bound to an owned configuration containing the pinned public store/cache paths. Its independent audit verifies all 331 committed source pins, raw test/report/receipt, namespace policy and cleanup. The host home, account and shared configuration were not changed. This diagnostic remains scoped to `1c386ec`.

The four-file unborn/state-object repair is committed only in a fresh isolated verification checkout at `2fa66568407594523380ec33c063cdf52e4c6d2d`, tree `a66441b9156667a0a18d44a102f30caad9ee9c3b`, a direct child of `1c386ec`. All nine source commands completed there with original deadlines and a finite eight-hour bound: 1076 controller tests, 1250 root tests across 101 files, and the consumed build passed. Collection rechecked all 331 source pins and clean identity after the owned service/cgroup disappeared. The independent precommit observer passed, validating the actual reports, Git inputs and same-input C3 host/OCI/cleanup audits. The separate C3 lifecycle passed at 553687ms, including all six actual OCI cases; it dispatched no long source suite.

The complete native publication run at the dirty primary input finished with 258 PASS / 8 FAIL / 266, including five test and three hook deadlines at the unchanged 180000ms bounds. Its raw diagnostics and ERROR manifest remain failed. A clean serial retry was invalidated by the observation launcher's inherited stage/command identifiers, which mislabeled nested review receipts. It was stopped after ten partial cases (four pass, six fail); no full report or PASS receipt exists. Independent retained inspection validates all 37 files, exact receipt-ID errors, clean source pins, byte-preserving collection and targeted stop observation. The earlier overbroad process inventory remains private outside curation. The corrected third attempt uses the same clean `2fa6656` source, all ten files and original deadlines. The retained auditor requires its full 266-case PASS, clean before/after pins, real child receipt, unchanged raw identities and exactly matching executed procedures. The primary C6e completion commit remains separate from these isolated verification inputs; post-commit, exact hosted and real migration obligations retain their own evidence.

Native Windows dependency verification also passed from a fresh owned copy installation at `1c386ec`, including all seven actual reference/log meanings and 192 graph entries / 138 installed packages. The primary installation's earlier rejection remains recorded: its ESLint file had 19 hard links, violating the unchanged independent-copy rule. The fresh installation uses the normal frozen offline copy command. This confirms the native dependency boundary and leaves native workflow qualification incomplete.

The evidence audit also preserves failed recovery/deadline runs, the honest non-passing full/focused native observations against synthetic publications, clean parent post-commit artifacts and exact parent hosted run `34047573294` with five successful jobs and 43 validated receipts. These observations retain their own commit and runtime identities.

After curation, reproduce the retained audit from the clean C6e commit with Node 24.18.0 and pnpm 11.15.1. The existing extractor checks the archive inventory and hashes before creating a fresh owned directory. The auditor rechecks every retained file and receipt artifact, reconstructs the frozen Git input and independently reconciles raw test identities. It audits retained observations; it does not rerun the VM, tests or hosted jobs.

The seal retains exact copies of the active plan, autonomy log and decision log under `operational-records/`. The auditor checks those archived bytes against the manifest. The live handoff and logs may then record later results without altering the seal. Executable source, tests, configuration and retention procedures remain pinned to the checked implementation; the original 650-file frozen reconstruction and every acceptance gate are unchanged.

```powershell
$env:PATH=(Resolve-Path '.tools/node-v24.18.0-win-x64').Path+';'+$env:PATH
$taskManifest=Get-Content -LiteralPath 'docs/source-authority/ORCH-AUTH-01-C6e/evidence/manifest.json' -Raw | ConvertFrom-Json
& 'docs/ci-regressions/wp6e-inventory/extract.ps1' -Archive 'docs/source-authority/ORCH-AUTH-01-C6e/evidence/retained-evidence.zip' -ExpectedSha256 $taskManifest.archive.sha256 -Destination (Join-Path (Get-Location).Path 'artifacts/c6e-fresh-retained-input')
pnpm --config.verify-deps-before-run=error exec tsx docs/source-authority/ORCH-AUTH-01-C6e/audit.ts artifacts/c6e-fresh-retained-input artifacts/c6e-fresh-retained-audit
```

The authorized continuation proceeds through the real audit/request/review/activation, a qualified finite host preserving the 3,900,000 ms partition allowance, the actual eleven-command candidate with four independent partition receipts and exactly-once reconciliation, and committed 5(a)/5(b) rejection proofs. Historical WP6e remains BLOCKED at `e590e38c32de2b5baa7423f66bbd8a0230b61839`; root-unit success does not establish unit-domain success. Linux, native Windows, Windows Server 2022, readiness and human acceptance remain separate gates. C6e makes no WP6f performance or keep/revert claim.
