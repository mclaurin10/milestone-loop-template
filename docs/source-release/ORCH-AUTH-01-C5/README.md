# C5 — consumed portable source build and real dependency/import checks

C5 implements an ordinary source build under the still-active `legacy-source.v1` authority. The existing `pnpm build` argv is unchanged. Its new explicit productionBuild declaration invokes an implemented source release command, builds committed Git bytes in the existing clean disposable clone, and retains the actual archive, manifest, detached consumer and child receipt before cleanup. The complete original output-containment, stale-output, outside-mutation and second-inventory checks remain in force.

The portable payload has 173 files/2,663,446 bytes: allowlisted TS/tsx runtime, schemas, scaffold, generation definition and notices, with a frozen dependency lock. SOURCE.json identifies its actual source commit/tree and grants no adopter authority. The extracted CLI installs its own dependencies in frozen offline copy mode and generates a clean, independent two-commit bootstrap adopter. Generated adopters receive the original universal AGENTS bytes from a dedicated template; source contract/epoch/commissioning/readiness/state do not carry into their identity.

| Actual supporting source | Observed result |
| --- | --- |
| `6dd6463cf630176783c084dda4537386021045bd`, tree `66598b41f04e84d719ac5eb394b842d511ed81b6` | Real build and architecture PASS. Archive 559,124 bytes, SHA256 `b8f33ddfdad6547a00f4cc3938865b60f3d9fc430b69924520d173f3b7bd070d`. Dependency comparison correctly remained failed until its pnpm workspace-link representation was handled. |
| `c3617cb5be2c05203a3a2e5205396c2242c4f118`, tree `a7a0d2355bd1b9f138bc34f4cfcea30d8f1e39f1` | Real build, full dependency comparison and 79 affected tests PASS. Archive 559,123 bytes, SHA256 `e38003ea2f1e43ac6e2b4d6eef53d9c9168aefbf91b2ebf2e89b843ce94b1d80`. |

Both builds ran native Windows with Node 24.18.0/pnpm 11.15.1 and independent copy-mode installations. Their build-command durations were 28,474 ms and 37,934 ms; these are observations only. Different source commits mean these are not the same-source repeated builds required for ORCH-BUILD-01. Python 3.12's standard tar reader independently inspected every entry of each archive from Linux/WSL; that observer is not a native Linux build.

The dependency checker validates exact runtime/lock/store, obtains the installed graph and an independently installed frozen offline reference, then compares every present package's file inventory. The final result covered 192 graph entries, 138 present packages and 3,431 files/543,736,116 bytes. A pnpm `link:` alias is accepted only from a declared workspace when it resolves to the contained package path and its installed name/version agree; byte comparison remains mandatory. The architecture checker parses actual imports, entrypoints and literal assets, keeps shipped imports closed over shipped files, and applies the generator's actual scaffold overlay layout. Missing runtime/schema/scaffold, unsafe archive, import-boundary, changed dependency and receipt/artifact regressions are executable.

The initial focused run retained five failures among 78 cases. The misplaced adopter argument and catalogue ordering were fixed; the old absence-of-build test now uses an explicitly undeclared fixture while retaining all its original exit-2/no-PASS/NOT_READY assertions and deadline. The later alias case brings the final total to 79. Initial typecheck/import diagnostics and the failed dependency command remain non-passing. The first Python inspection had a required-name case-comparison bug; its executed script and accurately labelled tool-output transcription are retained, with no fabricated raw stderr or PASS receipt. Corrected independent inspections pass. Static checks and all five invariants pass; final invariants took 64,073 ms, above the advisory warm target without changing it.

`evidence/retained-evidence.zip` has 318 regular files and 29 validated receipts, 1,571,441 bytes, SHA256 `2bb249859bd63731956b76a753ab87ba5cf88154e176f914a454c3f96a555763`. The immutable inventory pins the actual supporting commits, implementation and unchanged active generation. The auditor reconstructs both archives from their Git objects, independently validates every raw receipt/artifact and consumer, and compares the Python inventories. Fresh extraction/audit passed before and after integrating repair `42871f6`; the latter observes C4's snapshot as a strict ancestor while activation remains false.

Use the exact C5 commit and Node/pnpm pins, with fresh output paths:

```powershell
& docs/ci-regressions/wp6e-inventory/extract.ps1 -Archive docs/source-release/ORCH-AUTH-01-C5/evidence/retained-evidence.zip -ExpectedSha256 2bb249859bd63731956b76a753ab87ba5cf88154e176f914a454c3f96a555763 -Destination (Join-Path (Get-Location) 'artifacts/c5-fresh-extraction')
pnpm exec tsx docs/source-release/ORCH-AUTH-01-C5/audit.ts artifacts/c5-fresh-extraction artifacts/c5-fresh-audit
```

A fresh build requires a clean committed checkout and a prepared frozen copy-mode install. Run `pnpm build`, `pnpm verify:source-dependencies`, and `pnpm lint:source-architecture`; give each a fresh LOOP_VERIFY_COMMAND_ARTIFACT_DIR. The outer build receipt retains the distribution under its `source-release/` directory. Actual post-commit build/check/audit observations remain required after this evidence commit; retained replay is not a fresh build.

C5 paused for the failed 827ebbf hosted cohort and resumed after the narrow canonical-fixture repair `42871f66ae1711821703f16bcb97cca18b5ccf3a`. All 24 C5 files and the user's roadmap hashes matched after ordinary stash/fast-forward/conflict recovery. The recovery stash is retained. New exact repair run [34015911019](https://github.com/mclaurin10/milestone-loop-template/actions/runs/34015911019) is pending; do not cancel it with a later master push before retaining its outcomes.

No source authority activation, full ORCH-BUILD-01 qualification, native Windows Docker workflow, actual candidate-tier run, 5(a)/5(b), readiness, human acceptance or WP6f claim is supplied here. The synthetic output-retention receipt tests are not the required real candidate mutations. Historical WP6e remains BLOCKED at e590e38. Continue strict compatible consumers and the approved separately committed/reviewed/leased/recoverable migration, then the actual clean candidate and remaining mutation obligations. No source controller state was initialized or adopted.
