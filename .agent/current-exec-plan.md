# Current Execution Plan

Status: Locally verified; commit and exact post-commit/hosted evidence pending. Updated: 2026-09-06 UTC. Owner: authorized WP6e continuation, exact hosted regression repair.

## Objective

Repair the eight launch-free host-discovery fixture failures exposed by exact Windows Server 2022 run 34012032506 at 827ebbf, preserving the scanner's strict canonical-path refusal and every original assertion. C5 source build work is paused and retained in the primary checkout; this isolated worktree starts at 91cbd3e.

## Goal Constraints

Keep the frozen placeholder/legacy authority, settled but inactive r2 approval, all protected workflows and deadlines, original test meanings and absent source state. No launcher execution or shared host/package/service changes. Historical WP6e remains BLOCKED at e590e38. Preserve the untracked roadmap and paused C5 files.

## Baseline Evidence

The repair cohort's Linux controller, both fresh adopters and Docker fixture passed. Windows controller suite passed; its complete unit run failed eight host-discovery cases (929/937 passed) because the test-owned TEMP root used an alias while the production scanner correctly refused noncanonical inputs. All five provider artifacts are downloaded and digest-checked under primary artifacts/wp6e-continuation-20260906. The raw unit report and decoded Windows log are retained there.

## Steps

1. Complete: reproduced exactly eight failures/32 passes in the original forty cases under an uppercase native Windows TEMP/TMP alias, with no PASS receipt. The initial redirection setup failed before launch and is separately recorded as NOT_EXECUTED.
2. Complete: canonicalized only the newly created fixture root. All 128 repository-tooling cases pass under the same uppercase TEMP/TMP; typecheck, lint, format and five invariants pass (44,836 ms).
3. In progress: sealed 113 files/ten receipts and independently audited fresh extraction, matching all eight actual hosted/local failures and every unchanged assertion/scanner byte. Commit the narrow repair, run exact post-commit checks and dispatch the five-job cohort. Integrate by ordinary Git recovery preserving every paused C5 byte, then resume its committed build closeout and remaining migration/candidate obligations.

## Acceptance Criteria

The real alias reproduction reaches the same eight failures. The corrected canonical fixture passes every original case, including actual installer-shaped script non-execution, directory/binary observation and link/alias refusal. No production scanner behavior or original deadline is weakened. The complete applicable tooling suite and required checks pass with command-owned evidence.

## Verification

Receipt-owning focused runner with TEMP/TMP set to an existing uppercase alias; full repository-tooling owner, five invariants, typecheck/lint/format. Post-commit repeat and independent retained audit. Preserve exact failed/passing reports and the new five-job hosted cohort. No GUI or simulation changes.

## Risks and Recovery

The prior repair reached only the controller suite, so this later full-unit discovery is distinct from its earlier 25 input-fixture failures. Never relabel the failed cohort as passed. Use realpath only for an owned test fixture; do not normalize arbitrary discovered launchers. C5 remains uncommitted and preserved outside this repair. Recover through ordinary source control, without reset, adoption, deletion of the user's roadmap or authority changes.

## Progress and Evidence

The executable reproduction and retained audit pass as described in docs/ci-regressions/host-discovery-path/README.md. Archive SHA256 daa7575dfa33966dba915c0779475f17b4365e0506b7f087885231854b80c97d. The old cohort remains failed. C5 work is preserved outside this isolated increment; no source state or authority changed.

## Next Action

Commit, rerun the original forty cases with the native Windows TEMP alias and audit the committed evidence, then push the exact candidate for all five protected jobs. Restore C5 by ordinary Git integration and continue its full authorized loop; do not stop at this repair.
