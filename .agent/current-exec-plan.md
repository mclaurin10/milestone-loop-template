# Current Execution Plan

**Status:** Complete — candidate-prepare and the omitted WP2 recovery boundary are closed
**Updated:** 2026-08-24
**Owner:** autonomous loop

## Objective

Finish the omitted WP2 `candidate-prepare` operation as one complete,
intent-first, fail-closed recovery boundary. Audit every real canonical,
Worker, Git, artifact, and mirror boundary; make every attributable
interruption converge through the normal semantic reducer; preserve and block
every ambiguous observation; prove controller-lease/CAS exclusion under
synchronized resume; finish strict schema/migration/transition coverage and
read-only status/Doctor projections; run the required focused and broad exact
runtime verification; and create one cohesive local commit only when every
criterion is observed.

This plan may close `candidate-prepare` and the previously omitted WP2 recovery
boundary. It does not begin WP6, CAL-1, hidden validation, product work,
readiness implementation, product completion, autonomous readiness, or human
acceptance. It does not change Planner/Worker/Reviewer separation, execution
provider eligibility, verification, integration, cleanup, retention, receipt,
timeout, protected-root, or readiness semantics. It must not run source
no-argument `pnpm verify`, invoke `loop:template:prove`, push, force-push, or
alter frozen authority/acceptance files.

## Goal Constraints

- Preserve `PROJECT_GOAL.md`, `AGENTS.md`, the original acceptance contract,
  immutable-lock baseline/active meanings, the permanent readiness transition,
  and CAL-1 `open_not_started` state.
- Use exact Node `24.18.0` from `.tools/node-v24.18.0-win-x64` and pnpm
  `11.15.1`. Prepend the pinned Node directory to the complete inherited child
  `PATH`; reject evidence produced by ambient Node.
- Preserve untracked human file
  `Implementation-ready improvement plan 8-5-26.txt` byte-for-byte at SHA-256
  `53ea98fb1cb880163a02d3b1d9365963e3fe891025ae3630f00bd4c9232293b1`.
  Preserve unrelated user changes.
- Reuse the one exclusive `pendingOperation` union and canonical Git-ref state
  CAS. Canonical state alone authorizes recovery; Worker events,
  `worker-turn.json`, `controller-checkpoint.json`, and the state mirror are
  derived evidence only.
- Keep the Session 1 operation bound to exact input generation/revision, run,
  milestone, attempt, repository/target/workspace/base/branch, workspace-create
  identity, starting candidate/commits, Worker role/assignment/policy/thread
  lineage, retry/proposal/protected context, evidence paths, candidate/
  checkpoint facts, phases/timestamps, and validate/resume/adopt-or-preserve
  policy.
- No Worker gateway entry may occur before the exact intent and invocation-start
  authority are canonical. No candidate may reach verification until complete
  path, standalone Git, ancestry, cleanliness/index, protected-file,
  diff-policy, checkpoint, artifact-path, and Worker-context validation passes.
- Preserve every unexpected, external, or ambiguous workspace, commit, index,
  artifact, and path in place. Never reset, clean, delete, move, recommit,
  silently adopt, or use a derived artifact as authority.
- Add only explicit candidate phases/hooks that correspond to real durable or
  external boundaries. Do not build a generic workflow engine or a second
  journal.
- Existing `workspace-create`, `target-integrate`, `workspace-cleanup`, and
  `retention-apply` reducers, migrations, recovery, projections, and receipts
  must remain passing.

## Baseline Evidence

- Collision gate: the Codex task `Define candidate prepare recovery`
  (`01a031d5-8e1c-7cb3-90f5-f29c285cd75c`) has a durable `task_complete` event
  at `2026-08-24T05:46:35.937Z`. No repository-matching writer process or Git
  lock file was present before this plan edit.
- Session 1 is committed locally as
  `069527e92744149f0041dcaa3c978cf773adace9`, tree
  `9972099e5f47a4246961dc81a01f869a4bf4662d`, parent/origin
  `96c3eb2da170da5eed4cf99dc0becf5eb256d138`. Before this plan edit the human
  plan was the sole worktree entry and matched its required digest.
- Session 1 advanced state schema from `1.9.0` to `1.10.0`, added the fifth
  operation kind, published intent before Worker mutation, unified normal and
  authorized post-checkpoint completion, and replaced no-intent clean
  descendant adoption with preservation/blocking.
- Truthful red evidence is retained under
  `artifacts/manual/candidate-prepare-session1-red-v5/`: the deliberately
  failing report is 0/2; checkpoint-crash and out-of-band observations have
  SHA-256 `e7e56ba987b90d02cd86bc36a7f4fd8626f76d4ee2d3657d10b9fa8459ff49ed`
  and `1280fdd8eb5282787abafc5ddd7b09a7957a1c0629e709ad1cbaf878b28926d0`.
- Session 1 green receipts report 30/30 suites and 110/110 tests at
  `artifacts/manual/invariant-vitest-3240/`, followed by 10/10 suites and 18/18
  tests at `artifacts/manual/invariant-vitest-16196/`, with zero failures,
  skips, pending tests, or todos. Typecheck, lint, and format receipts are under
  `artifacts/manual/typecheck-9636/`, `artifacts/manual/lint-15348/`, and
  `artifacts/manual/format-check-21424/`. Their displayed commands use the
  pinned Node executable.
- Existing candidate fault points cover state after intent, invocation, thread,
  Worker completion, Worker artifact, Worker evidence state, checkpoint plan,
  checkpoint commit, checkpoint state, checkpoint artifact, checkpoint state,
  and completion. The first unresolved boundary is recovery from
  `intent-persisted`; the current inspector deliberately blocks it and all
  pre-Worker-completion phases as `worker-outcome-ambiguous`.
- Current production gaps observed during inspection: canonical Worker
  completion lacks enough bytes to regenerate a missing derived Worker-turn
  artifact; evidence file/ancestor substitution is not validated as strictly as
  the workspace; the hard-loss/repeated-resume/concurrent-resume matrix and
  full adversarial classification breadth do not exist; dirty no-intent output
  and read-only byte preservation are not comprehensively proven; and Session 1
  ran focused/static checks only, not the required broad Session 2 commands.

## Steps

1. [x] Complete collision/handoff and authority inspection. Read the frozen
       goal, agent/plan contracts, Session 1 plan/log/decision/commit/diff and
       retained receipts, WP2/WP6 improvement-plan sections, Git identities,
       runtime pins, current state/schema/operation owners, and exact next
       action. Verify the protected human-plan digest.
2. [x] Audit phases/hooks against every real boundary and add
       a deterministic hard-process-loss owner plus only the missing explicit
       candidate phase/data/path safeguards. Prove safe resume from exact
       pre-launch intent without duplicate gateway entry; preserve ambiguous
       launch/thread/mutation/return windows; make exact canonical Worker and
       checkpoint evidence regenerable; cover checkpoint start/commit/
       publication/completion and the state store's post-CAS mirror-failure
       boundary. For each automatic interruption, resume twice and compare the
       semantic result with uninterrupted execution, ignoring only declared
       timestamps/diagnostic IDs.
3. [x] Add synchronized two-contender recovery under the real controller lease
       and state CAS. Exactly one contender may advance; the loser must observe
       or fail safely. Assert one Worker launch, one controller commit, one
       authoritative artifact instance, one verification transition, and one
       operation completion.
4. [x] Complete the fail-closed matrix: clean/dirty no-intent descendants;
       unexpected commit/parent/tree/base or active Git-operation drift;
       missing/linked/reparse/substituted workspace/evidence paths; protected
       and diff-policy violations; conflicting/substituted Worker/checkpoint
       evidence; and Worker role, assignment, policy, retry, attempt, or thread
       mismatch. Prove no unowned candidate reaches verification and an exact
       operation-authorized checkpoint is adopted only after the full shared
       validation boundary.
5. [x] Finish runtime and shipped JSON Schema validation, any required prior
       state migration, operation-union exhaustiveness, immutable-field
       transition fencing, and compatibility tests for all four earlier
       operation kinds. Do not fabricate recoverability for legacy ambiguous
       candidate state; migrate it to an explicit preserved block when needed.
6. [x] Complete inspection, `loop:status`, and `loop:doctor` projections for
       kind, phase/progress, classification/disposition, preserved paths, and
       exact next safe action. Snapshot canonical refs, index, worktree, object
       database, workspace, state mirror, and derived evidence before/after to
       prove these reads are byte-preserving.
7. [x] Run pinned receipt-owning candidate/reducer/schema/orchestrator/fault/
       status/Doctor owners with serial Vitest files and unique ignored roots.
       Then run every required affected regression and broad command. Inspect
       receipts, report hashes, exact runtime, test/suite counts, skips/todos,
       candidate binding, retained fault evidence, protected identities, and
       worktree state independently; fix root causes without weakening tests or
       limits.
8. [x] Update `CONTRACT.md`, `README.md`, this plan,
       `docs/autonomy-log.md`, and
       `docs/decision-log.md` only with observed behavior and durable decisions.
       Record `candidate-prepare` and WP2 closed only after all criteria pass.
       Recheck immutable/human-plan identities and staged paths, then create one
       cohesive local commit. Do not push.

## Acceptance Criteria

- Durable `candidate-prepare` intent exists before Worker gateway entry or
  workspace mutation, and the canonical invocation-start state precedes the
  actual gateway call.
- An exact unchanged `intent-persisted` operation resumes the Worker safely;
  every ambiguous post-launch/pre-completion window preserves and blocks rather
  than relaunching. No tested path duplicates Worker launch or effects.
- Every automatic hard-loss point converges with uninterrupted completion
  through the same reducer after two resumes. Differences are limited to an
  explicit allowlist of timestamps/diagnostic IDs; Worker launch, commits,
  candidate/artifact authority, verification transition, and intent completion
  occur exactly once.
- Two synchronized resume contenders cannot both advance the operation. The
  controller lease/CAS winner completes and the loser observes or fails safely,
  with no duplicate external or canonical effect.
- Clean and dirty unowned candidates, unexpected Git facts, unsafe/substituted
  paths, policy/protected drift, conflicting artifacts, and Worker-context
  mismatches are classified exactly, preserved in place, and never reach
  verification.
- An authorized checkpoint is adopted only after complete workspace/path/Git/
  protected/diff/Worker/artifact validation. Missing derived evidence is
  regenerated only from sufficient canonical state; derived bytes never create
  authority.
- Strict runtime and shipped JSON schemas agree, malformed candidate variants
  fail, prior supported state migrates safely, operation union dispatch is
  exhaustive, immutable fields cannot change outside canonical reducers, and
  all four earlier operation kinds remain compatible.
- Inspection, status, and Doctor expose operation kind, phase/progress,
  classification/disposition, preserved paths, and exact next action while
  leaving refs, index, worktree, object database, workspace, mirror, and derived
  evidence byte-identical.
- Focused owner receipts and every required broad command pass under Node
  `24.18.0` / pnpm `11.15.1` with zero unexpected skips, timeouts, flakes, or
  missing receipts. The protected human plan and frozen identities remain
  unchanged.
- `CONTRACT.md`, `README.md`, this plan, and durable logs match observed
  behavior. One cohesive commit exists locally; no push, WP6, CAL-1, hidden
  validation, product/readiness work, no-argument verify, or template proof
  occurs.

## Verification

Every Node/pnpm command runs in a fresh PowerShell process. Resolve
`.tools/node-v24.18.0-win-x64`, prepend it to the complete inherited `PATH`,
assert `node --version == v24.18.0` and `pnpm --version == 11.15.1`, and use
unique ignored `LOOP_VERIFY_COMMAND_ARTIFACT_DIR`,
`MILESTONE_LOOP_TELEMETRY_RUN_ID`, `TEMP`, `TMP`, `COREPACK_HOME`, and
`pnpm_config_store_dir` roots where applicable. Focused Vitest ownership uses
`--fileParallelism=false`.

Planned receipt-owning focused owner (adjust file list only for actual directly
affected owners):

- `node tools/run-tool-evidence.mjs invariant-vitest tools/milestone-orchestrator/src/candidate-prepare.test.ts tools/milestone-orchestrator/src/candidate-prepare-recovery.test.ts tools/milestone-orchestrator/src/candidate-prepare-baseline.test.ts tools/milestone-orchestrator/src/operation-intent.test.ts tools/milestone-orchestrator/src/schema.test.ts tools/milestone-orchestrator/src/state-store.test.ts tools/milestone-orchestrator/src/status.test.ts tools/milestone-orchestrator/src/doctor.test.ts tools/milestone-orchestrator/src/cli.test.ts --fileParallelism=false`
- Add reconciliation, candidate identity, lifecycle/cleanup/retention, lease,
  path-safety, and existing-operation recovery owners when their shared boundary
  is affected; do not substitute an aggregate for focused diagnosis.

Required broader/static closure:

- `pnpm typecheck`
- `pnpm lint`
- `pnpm format:check`
- `pnpm test:orchestrator`
- `pnpm test:unit`
- `pnpm loop:demo-safety`
- `git diff --check`

Independently parse every receipt/manifest/report and retained fault-matrix
record; verify artifact hashes and exact displayed runtime, suite/test totals,
zero unexpected skips/todos, candidate commit/tree/dirty binding, operation and
checkpoint identity, and protected/worktree bytes. Source no-argument
`pnpm verify`, `loop:template:prove`, hidden validation, CAL-1, and WP6 remain
prohibited.

## Risks and Recovery

- Recovery from `intent-persisted` is safe only because the canonical
  invocation-start transition occurs before gateway entry. Tests must make that
  ordering executable and must preserve every later ambiguous phase; derived
  events cannot prove a launch did or did not occur.
- Legacy `1.10.0` canonical Worker completion stores hashes but not enough
  material to recreate a missing Worker-turn artifact. The `1.11.0` migration
  must continue to block that legacy pending state rather than inventing bytes
  or trusting events.
- Artifact ancestors can be substituted after intent publication. Validate
  lexical/real containment and regular-file identity before reads or writes;
  never follow a link outside the operation-owned attempt path.
- Child hard-loss and two-process lease tests are expensive and susceptible to
  host sleep. Treat interrupted/timed-out runs as invalid, retain diagnostics,
  and rerun unchanged commands awake; do not alter repository timeouts to hide
  failure.
- A broad regression stops closure. Preserve safe work and diagnostics, record
  the exact first failure, fix the root cause, and rerun affected broader
  owners. Ordinary Git history is the rollback boundary; never reset or clean
  the protected human file or ambiguous candidate workspaces.

## Progress and Evidence

- 2026-08-24: Collision gate passed from durable local task history after the
  app task-list/read tools did not return. Predecessor Session 1 is complete,
  its commit is current HEAD, no active repository writer or Git lock was
  found, and the protected human plan hash matches.
- 2026-08-24: Required authority, plan/log/decision, WP2/WP6, commit, retained
  evidence, runtime, schema, reducer, inspector, orchestrator, and state-mirror
  boundaries were inspected. Session 1 receipts and reports match their logged
  counts/hashes and identify the exact remaining Session 2 work above.
- 2026-08-24: State `1.11.0` now retains canonical redacted Worker response
  bytes, migrates legacy hash-only candidate completions to a preserved block,
  safely resumes only an unchanged pre-launch intent, regenerates missing
  derived evidence, validates evidence ancestors/files, and preserves unowned
  or conflicting evidence. Invocation/thread/gateway-return ambiguity remains
  deliberately non-replayable.
- 2026-08-24: Receipt-owning focused core evidence at
  `artifacts/manual/candidate-prepare-session2-focused-core-green-3/` passed
  40/40 tests with zero failures or skips. Retained hard-loss evidence at
  `artifacts/manual/candidate-prepare-session2-fault-matrix-red-2/` passed 8/8
  tests and records all 16 fault points under Node `v24.18.0`: 13 automatic
  convergences and 3 preserved ambiguous outcomes. The same owner also proved
  clean/dirty/unowned blocks, exact Git/path/policy/artifact/context
  classifications, and byte-identical inspection/status/Doctor reads.
- 2026-08-24: A synchronized two-process diagnostic passed under the real
  controller lease/CAS in 63.7 seconds: exactly one contender emitted recovery
  hooks; the other observed or failed safely; final state, evidence hashes, and
  one-commit workspace remained unchanged on a repeated resume. The next broad
  receipt-owning orchestrator run must execute this owner without test-name
  filtering before it qualifies for closure.
- 2026-08-24: The complete unfiltered candidate baseline owner now passes 9/9
  tests with zero skips/todos at
  `artifacts/manual/candidate-prepare-session2-fault-matrix-final-2/`. Its
  receipt/report SHA-256 values are
  `0d2f061747e2056b35c57ea05d4505a5ea400c56ca699d591e3ff2d48bf993d8`
  and
  `e12cf3db4771fb9889211860a9691347abb49a3e6ea63366e2415894301de123`;
  the manifest spans 15 minutes 18.984 seconds and the JSON report records
  915,222.353 ms of assertion time. This command exercised all 16 hard-loss
  rows and the synchronized lease owner, but the separate matrix-output
  environment variable was omitted, so this receipt does not retain a fresh
  standalone matrix JSON.
- 2026-08-24: The exact combined non-crash/compatibility command recorded in
  `artifacts/manual/candidate-prepare-session2-focused-closure-final-3/manifest.json`
  passed 56/56 suites and 203/203 tests with zero failures, skips, pending
  tests, or todos. Its manifest spans 24 minutes 6.836 seconds and the report
  records 1,435,719.655 ms of assertion time. Receipt/report SHA-256 values are
  `c8aa59afee3b15b01d42c60d985bbca284cc75badf56a3bdf4d4f6b626eec9ae`
  and
  `63850bdb790bcf0e6781a2cff2269638cab6d8db3ede90261236168406519b59`.
  The dirty candidate binding is HEAD
  `069527e92744149f0041dcaa3c978cf773adace9`, tree
  `9972099e5f47a4246961dc81a01f869a4bf4662d`; this is focused iteration
  evidence, not completion eligibility.

## Session 2 Durable Checkpoint — 2026-08-24T07:15:13.942-06:00

### Changes Since the Previous Checkpoint

- Candidate completion now retains the redacted response bytes needed to
  regenerate canonical Worker-turn evidence; strict `1.10.0` to `1.11.0`
  migration blocks unrecoverable legacy pending operations. Runtime and shipped
  schemas, transition fencing, inspectors, status, Doctor, CLI projections,
  normal/recovered reducers, evidence path validation, preflight preservation,
  and the adversarial/fault/concurrency owners were completed as described in
  Steps 2 through 6.
- The hard-loss worker now uses a real asynchronous child process and bounded
  crash batching. The matrix still invokes every real hard-loss point, resumes
  every authorized case twice, compares it with uninterrupted semantics, and
  retains the three deliberately ambiguous outcomes as preserved blocks.
- Continuous same-lease recovery now avoids redundant full worktree inspection
  between controller-owned state/evidence transitions; every fresh restart
  still performs the full inspector, and checkpoint preparation/commit retain
  the complete Git/path/policy validation boundary.
- State publication no longer rewalks an already validated immutable pending-
  operation lineage after an exact fenced successor wins canonical CAS.
  Mutation opens and read-only/fresh opens still validate the full lineage.
  This removed quadratic multi-phase persistence without changing the CAS,
  generation parent, transition, mirror, or recovery authority.

### Exact Passing Commands and Evidence

- `node tools/run-tool-evidence.mjs invariant-vitest tools/milestone-orchestrator/src/candidate-prepare-baseline.test.ts --fileParallelism=false`
  under asserted Node `v24.18.0` / pnpm `11.15.1`: PASS, 2/2 reported suites,
  9/9 tests, 0 failed, 0 skipped/pending/todo. Evidence:
  `artifacts/manual/candidate-prepare-session2-fault-matrix-final-2/`; manifest
  duration 15:18.984, assertion duration 915,222.353 ms. The command did not
  set `CANDIDATE_PREPARE_FAULT_MATRIX_OUTPUT`, so a fresh standalone matrix
  record remains required.
- The exact 23-file command is preserved byte-for-byte as `displayCommand` in
  `artifacts/manual/candidate-prepare-session2-focused-closure-final-3/manifest.json`:
  `node tools/run-tool-evidence.mjs invariant-vitest <23 recorded owner files> --fileParallelism=false`.
  PASS under asserted Node `v24.18.0` / pnpm `11.15.1`, 56/56 reported suites,
  203/203 tests, 0 failed, 0 skipped/pending/todo; manifest duration 24:06.836,
  assertion duration 1,435,719.655 ms.
- After the latest state-store change, the non-receipt diagnostic
  `pnpm exec vitest run tools/milestone-orchestrator/src/candidate-prepare-baseline.test.ts --fileParallelism=false -t "recovers a controller checkpoint only when a matching intent survived process loss" --reporter=verbose`
  passed its selected test in 45,567 ms with eight expected filter skips. This
  is diagnosis only, not qualifying closure evidence. `pnpm exec tsc -p
  tsconfig.tools.json --noEmit` also exited zero in the same pinned shell.
- Before the latest performance edits, receipt-owning `pnpm typecheck`, `pnpm
  lint`, and `pnpm format:check` passed under
  `artifacts/manual/candidate-prepare-session2-static-typecheck-1/`,
  `...-static-lint-1/`, and `...-static-format-check-2/`. They are stale after
  later production/test edits and must be rerun.
- The protected untracked human plan still has SHA-256
  `53ea98fb1cb880163a02d3b1d9365963e3fe891025ae3630f00bd4c9232293b1`.

### Exact Non-Passing Commands and Retained Diagnostics

- The earlier eight-owner receipt command recorded in
  `artifacts/manual/candidate-prepare-session2-focused-owners-final-1/manifest.json`
  was non-passing: 20/22 reported suites and 96/97 tests passed, 1 failed, 0
  skipped, over 1,584,238.322 ms of assertion time. The failure was the stale
  Doctor expectation for `workspace-identity-mismatch`; production correctly
  classified the missing workspace as `workspace-path-missing`. The expectation
  was corrected and the final 203-test receipt above passes.
- The 16-file compatibility command recorded in
  `artifacts/manual/candidate-prepare-session2-focused-compat-final-1/manifest.json`
  was non-passing: 24/36 suites and 92/115 tests passed, 23 failed, 0 skipped.
  Every failure was a Windows fixture path-length setup defect from the long
  unique TEMP root. The unchanged command passed 36/36 suites and 115/115 tests
  under short root `C:\cp2\s2cf2` in
  `artifacts/manual/candidate-prepare-session2-focused-compat-final-2/`.
- The candidate baseline command in
  `artifacts/manual/candidate-prepare-session2-fault-matrix-parallel-final-1/`
  was non-passing: 7/9 tests passed, 2 failed, 0 skipped over 900,714.134 ms.
  One late crash child and the checkpoint regression exceeded their unchanged
  60-second bounds. The root cause was quadratic canonical lineage revalidation
  after every durable phase plus excessive crash concurrency; the state-store
  and bounded-batch corrections above produced the final 9/9 pass.
- `pnpm test:orchestrator` through the receipt-owning wrapper at
  `artifacts/manual/candidate-prepare-session2-broad-orchestrator-final-1/`
  is non-passing. The fixed 3,600,000 ms child bound expired, final process
  cleanup completed after a manifest span of 1:18:35.858, and no report or PASS
  receipt exists. The retained candidate fault matrix has SHA-256
  `b2c91724d24e095261a38b13c08c80ead771a745f2aa6986583cfdb7b834817d`.
  This result predates the lineage-performance correction and must be rerun.
- Two pre-fix isolated checkpoint diagnostics timed out at the unchanged
  60-second test limit (64,036 ms and 66,012 ms observed including cleanup),
  each with one failed selected test and eight filter skips. They are retained
  only as causal diagnostics and were superseded by the 45,567 ms pass.

### Current Diagnosis and Remaining Acceptance

No known semantic focused failure remains. The current production diagnosis is
that candidate durability required many explicit canonical phases, while the
state store unnecessarily revalidated the entire already-proven lineage after
each successful same-process CAS. The inductive post-CAS correction preserves
fresh-open lineage validation and made the previously timing-failing owner pass.

Session 2 remains in progress. Still required: emit and independently inspect a
fresh standalone 16-row matrix from the current tree; rerun receipt-owning
`pnpm typecheck`, `pnpm lint`, and `pnpm format:check`; run and inspect `pnpm
test:orchestrator`, `pnpm test:unit`, `pnpm loop:demo-safety`, and `git diff
--check`; verify receipts, zero skips/todos, exact runtime/candidate binding,
protected/frozen/human identities, and working-tree/staged paths; update final
documentation/log closure; then create one cohesive local commit. No partial
commit exists or is authorized.

## Next Action

First rerun the complete nine-test candidate baseline receipt command with
`CANDIDATE_PREPARE_FAULT_MATRIX_OUTPUT` bound inside its unique command-owned
evidence root. Independently prove the retained JSON has Node `v24.18.0`, all
16 ordered fault points, 13 `automatic-convergence` and 3
`preserved-block` rows, exact semantic/candidate/artifact identities, and no
duplicates. Then rerun the required static and broad commands above; fix any
regression before final closure documentation and the single cohesive commit.

## Session 2 Durable Checkpoint — 2026-08-24T11:31:57.2929808-06:00

This checkpoint supersedes the preceding `Next Action` but preserves the
earlier evidence ledger. It was written at the first safe boundary after the
in-flight final-tree `pnpm test:unit` rerun exited. No partial commit was
created.

### Changes Since the Previous Checkpoint

- The fresh standalone hard-loss owner now emits its required matrix under
  `artifacts/manual/candidate-prepare-session2-fault-matrix-final-3/`. The
  independently inspected record contains Node `v24.18.0`, all 16 ordered
  fault points, 13 `automatic-convergence` rows, and the three deliberately
  ambiguous `preserved-block` rows. All automatic rows converge after two
  resumes with no duplicate Worker invocation, checkpoint commit, artifact
  authority, verification transition, or intent completion.
- Two process-supervisor tests that previously skipped on Windows now exercise
  real cross-platform process-tree termination. Windows asserts the existing
  `taskkill-tree-force` attempt and a detached grandchild; POSIX retains the
  process-group SIGKILL assertions. This removed both broad-suite skips without
  suppressing or conditionally accepting the behavior.
- The commissioning owner still rejects both missing and unrelated bases, but
  now performs the two assertions sequentially against one owned repository
  fixture instead of paying for two independent Git fixture constructions.
  Its selected test fell from the broad-load failure at 5,391 ms to 3,198.799
  ms in focused evidence. No timeout or assertion changed.
- No production behavior has changed since the previous checkpoint beyond the
  already recorded candidate recovery and state-store performance work. The
  latest changes are verification portability and fixture-cost corrections.

### Exact Passing Commands and Evidence Since the Previous Checkpoint

- `node tools/run-tool-evidence.mjs invariant-vitest tools/milestone-orchestrator/src/candidate-prepare-baseline.test.ts --fileParallelism=false`
  under asserted Node `v24.18.0` / pnpm `11.15.1`: PASS at
  `artifacts/manual/candidate-prepare-session2-fault-matrix-final-3/`; 2/2
  suites and 9/9 tests, 0 failed/skipped/pending/todo, 910,272 ms manifest
  runtime and 906,447.409 ms assertion time. Matrix/receipt/report SHA-256 are
  `f14bc61c521690ae902020cbd286c2f1ef41c14acd0baa45a167979949d4500c`,
  `ac9bfc211743299c4d2327395a6120b1fff244fcb885927c9485f32d7ef70207`,
  and
  `2165e47d660dd234439e74bbf1f0132f4a57fb730e0cb989983d4fb1425ede9e`.
- `pnpm test:orchestrator` under the exact pinned runtime: PASS at
  `artifacts/manual/candidate-prepare-session2-broad-orchestrator-final-3/`;
  184/184 suites and 619/619 tests, 0 failed/skipped/pending/todo, 3,299,024
  ms manifest runtime. Receipt/report SHA-256 are
  `9149d2ad565fcb4250a7ce20bdf664d6b8b0f5505ad7e70c76b7a6f4b61d72d4`
  and
  `e762a316c32b05ef48c2f5f82861c05013f3a1ec04c375c7b7ed60ed583f056c`.
  This is qualifying for the process-supervisor change but became stale when
  the later commissioning fixture edit changed the tree; it must be rerun.
- `node tools/run-tool-evidence.mjs invariant-vitest tools/milestone-orchestrator/src/process-supervisor.test.ts --fileParallelism=false`
  under the exact pinned runtime: PASS at
  `artifacts/manual/candidate-prepare-session2-process-supervisor-final-1/`;
  4/4 suites and 21/21 tests, 0 failed/skipped/pending/todo, 9,017 ms manifest
  runtime and 6,830.883 ms assertion time. Receipt/report SHA-256 are
  `d3b3df260e3efec3e498f333b0e9760d029cb8a06a667ded8704e1150a89cd07`
  and
  `9af400af3697b337edb05ce32a13c20d7c1f83bb0ed1ac175ae81394b4fcaf53`.
- `node tools/run-tool-evidence.mjs invariant-vitest tools/milestone-orchestrator/src/commissioning.test.ts --fileParallelism=false`
  under the exact pinned runtime: PASS at
  `artifacts/manual/candidate-prepare-session2-commissioning-final-2/`; 3/3
  suites and 13/13 tests, 0 failed/skipped/pending/todo, 88,660 ms manifest
  runtime; the previously failing selected test took 3,198.799 ms. Receipt/
  report SHA-256 are
  `e57fa9f4959e044d21d10c2933573feecd883bb9cc7049a7903a40361d0e8429`
  and
  `4a9235eaeaecf64f37daf58c4028bc66dda3c9f01525fbd45af4e26d1f56cc56`.
- Receipt-owning `pnpm typecheck`, `pnpm lint`, and `pnpm format:check` passed
  before the two latest test edits under
  `artifacts/manual/candidate-prepare-session2-static-typecheck-final-2/`,
  `artifacts/manual/candidate-prepare-session2-static-lint-final-2/`, and the
  recorded final format root. They are stale for closure and must be rerun.
- `git diff --check` had passed before the two latest test edits. The protected
  human plan was re-hashed at this boundary and remains exactly
  `53ea98fb1cb880163a02d3b1d9365963e3fe891025ae3630f00bd4c9232293b1`.

### Exact Non-Passing Commands and Retained Diagnostics Since the Previous Checkpoint

- `pnpm test:orchestrator` at
  `artifacts/manual/candidate-prepare-session2-broad-orchestrator-final-2/`
  exited zero and produced a PASS receipt after 3,119,233 ms, but is
  non-qualifying under this plan: 184/184 suites, 617/619 tests passed, 0
  failed, and 2 Windows process-supervisor tests skipped. The two owners were
  made substantive cross-platform and the final-3 run above passed 619/619
  with zero skips.
- `pnpm test:unit` at
  `artifacts/manual/candidate-prepare-session2-broad-unit-final-1/` is
  non-passing: manifest runtime 3,532,934 ms; 184/186 suites and 634/635 tests
  passed, 2 failed suite entries / 1 failed test, 0 skipped/pending/todo. The
  sole assertion failure was commissioning `rejects missing and unrelated
  commissioning bases`, which took 5,391 ms against the unchanged 5,000 ms
  per-test bound under full-suite load. No PASS receipt exists. The focused
  fixture-cost correction above retains both negative cases and passes in
  3,198.799 ms.
- The final-tree rerun `pnpm test:unit`, invoked with pinned repository Node
  and pnpm plus `TEMP=C:\cp2\bu2`, `COREPACK_HOME=C:\cp2\core-bu2`,
  `pnpm_config_store_dir=C:\cp2\store-bu2`, and evidence/telemetry root
  `candidate-prepare-session2-broad-unit-final-2`, is non-passing at
  `artifacts/manual/candidate-prepare-session2-broad-unit-final-2/`. The
  unchanged 3,600,000 ms evidence-child bound expired; the ERROR manifest spans
  3,602,512 ms (`2026-08-24T16:31:26.815Z` to
  `2026-08-24T17:31:29.327Z`). No test report and no PASS receipt exist, so no
  test total can be credited. The retained 16-row matrix exists at SHA-256
  `dc4750752bb2a5e47537b5c249b046d90dc945653a4f0a5e1edf21661d9762c3`;
  the ERROR manifest SHA-256 is
  `3d006e241b5745c0d54842e6b31a5211beb4ab754722453e4411eefbe3b3ef1d`.

### Current Diagnosis, Remaining Acceptance, and Precise First Unresolved Action

No focused semantic failure is known. The commissioning assertion failure has
a reproduced fixture-cost cause and a focused green correction. The present
blocking fact is broader: the serial `pnpm test:unit` command did not finish
inside its existing one-hour evidence-child bound after adding substantive
Windows supervisor coverage, and therefore produced neither a report nor a
receipt. Elapsed time alone cannot establish which remaining owner or teardown
consumed the margin, and this timeout is non-passing. Increasing the bound,
skipping owners, or crediting the partial matrix is prohibited.

Still required: identify the exact serial owner/teardown cost that pushes
`pnpm test:unit` beyond one hour; make the smallest root-cause performance
correction without changing behavior, assertions, or timeouts; rerun its
focused owner and then final-tree `pnpm test:unit`; rerun final-tree `pnpm
test:orchestrator`; rerun typecheck, lint, format check, demo safety, and diff
check; independently inspect every receipt/report/matrix and zero skips/todos;
verify frozen/protected/human identities and Git paths; update closure records;
and create the one cohesive local commit. Candidate-prepare and WP2 remain
open; no partial commit, push, WP6, CAL-1, hidden validation, product/readiness
work, source no-argument verify, or template proof occurred.

**Precise first unresolved action:** obtain owner-level wall-time attribution
for the same serial `pnpm test:unit` file order from its retained/focused
owners, beginning with the 15-minute candidate hard-loss owner and the newly
portable supervisor/commissioning owners. Determine the last completed owner
and highest removable fixture/teardown cost without rerunning the unchanged
one-hour aggregate blindly; then add a focused performance regression and fix
that root cause. Do not change any timeout.

## Session 2 Pre-Commit Closure Ledger — 2026-08-24T15:08:00-06:00

### Root-Cause Closure Since the Durable Timeout Checkpoint

- The completed broad-unit final-1 report proved that the candidate baseline
  owner dominated the aggregate at 992,208.833 ms. Git Trace2 on one
  uninterrupted candidate operation then recorded 368 Git processes and five
  repeated standalone-workspace identity sweeps. Each sweep used separate
  processes for top-level/Git/common directory, branch, six local markers,
  remote enumeration, and six active-operation paths.
- Candidate workspace identity now preserves the same fail-closed facts while
  using one `rev-parse` for top/Git/common/branch, one exact local-config query
  with missing/duplicate detection, one remote query, and direct marker checks
  under the already proven standalone `.git` directory. No validation boundary,
  classification, timeout, or acceptance assertion changed. Focused
  uninterrupted and adversarial diagnostics passed; the exact owner and both
  broad suites below passed with zero skips.
- Two discarded diagnostics are retained but not cited as green: bounded-
  parallel fixture provisioning increased the selected hard-loss owner to
  536,781.052 ms, and a cloned repository-template fixture increased the
  selected dirty-candidate owner to 15,472.890 ms. Both experiments were
  reverted before qualifying evidence. An observation-only duplicate state-
  reload removal remains because both real resume opens and all semantic/effect
  assertions are unchanged; it is not credited as the runtime root cause.

### Final Behavioral and Receipt Evidence

- Dedicated exact-tree fault owner:
  `node tools/run-tool-evidence.mjs invariant-vitest tools/milestone-orchestrator/src/candidate-prepare-baseline.test.ts --fileParallelism=false`
  PASS at
  `artifacts/manual/candidate-prepare-session2-fault-matrix-final-5/`; 2/2
  suites, 9/9 tests, 0 failed/skipped/pending/todo, 936,982 ms manifest and
  931,366.961 ms assertion time. Manifest/receipt/report/matrix SHA-256 are
  `87593572b3682b01f52369a78b2f24988b65d5f3983927295134d84b8ef97ab4`,
  `0b09967f47b0f2c09fd97e834cec57818202c881f96fafb9d8d422007e29a2e7`,
  `1c4342c8225d7e68009047a038fbc9d7b12e3d53d104696133db777d7d5e64f8`,
  and
  `3aef037641d2a4a9fa51d53903914872a1a6198d8bfdb4ac661c4db2fc71895c`.
  The matrix reports Node `v24.18.0`, all 16 ordered fault points, 13 automatic
  convergences, and 3 preserved ambiguous blocks.
- Exact recorded 23-owner focused command PASS at
  `artifacts/manual/candidate-prepare-session2-focused-closure-final-4/`;
  56/56 suites, 203/203 tests, 0 failed/skipped/pending/todo, 1,563,842 ms
  manifest and 1,551,512.220 ms assertion time. Manifest/receipt/report
  SHA-256 are
  `1da26824fdecbdf46de0e4ea141799ae0c42bdca8c38fee19a367e9427dd2f07`,
  `fec242a211b5d305b379acda662c4f5ab11147625e3621f28aed3a1f203027d9`,
  and
  `669852c4070eb48af598845b479db8133a8b9c3c53e2b571b5ec1f543c29b140`.
- Final-tree `pnpm test:unit` PASS at
  `artifacts/manual/candidate-prepare-session2-broad-unit-final-3/`; 186/186
  suites, 635/635 tests, 0 failed/skipped/pending/todo, 3,457,613 ms. Its
  receipt/report/matrix SHA-256 are
  `c469776611ad60d1cca228f39cf60159618f32d1caca8cdcdc2b92aebdcf68b8`,
  `13b46bdecc5efb78ebe537bfa760f672e4aa045629fe46e60f8dbff8ebaa335d`,
  and
  `e7c0b4153696094b3306a42fadaba874e86787c1ee60594e84adb074caf07678`.
  The matrix is 16 rows at 13 automatic/3 preserved under Node `v24.18.0`.
- Final-tree `pnpm test:orchestrator` PASS at
  `artifacts/manual/candidate-prepare-session2-broad-orchestrator-final-4/`;
  184/184 suites, 619/619 tests, 0 failed/skipped/pending/todo, 3,373,893 ms.
  Its receipt/report/matrix SHA-256 are
  `484bcc81be31df27f478a43adbaf9428d99e95aa42e4885baa7567ee1cdbb5b8`,
  `8de7d2f485bf80d966df99c2fd6ea695d1728a621f4d31b6d32f36df2ef676d3`,
  and
  `28e0b259fe278adc1dc5f770df685dfe1b8375f333ad365f5ad6904e874a666e`.
  The matrix is again 16 rows at 13 automatic/3 preserved under Node
  `v24.18.0`.
- `pnpm loop:demo-safety` exited zero with 6/6 PASS scenarios. Retained artifact
  `artifacts/orchestrator/runs/safety-demonstration/safety-demonstration-20260824210204430-31e69383.json`
  has SHA-256
  `e141e76d7de88d2895e56a8800252c2389bbe8c111bb8c626de20cbdc5b40ce3`.
- Final post-document `pnpm typecheck`, `pnpm lint`, and `pnpm format:check`
  PASS under
  `artifacts/manual/candidate-prepare-session2-static-typecheck-final-6/`,
  `...-static-lint-final-6/`, and `...-static-format-final-6/` in 11,613 ms,
  25,933 ms, and 26,566 ms. Receipt SHA-256 values are
  `5ba59474a2ec71d943a9ae12111a7c0abc6cc01abcb21b10c9b170a716e9d1f1`,
  `21b250e8a5a0921571bdc0df6dad934783db26d137ab78a423534bca7b58ddbc`,
  and
  `de65188c06f01e578cf371152fbbc84454a2c81b0d150ba9668ef7e14f3a3f0a`.
  The independently rerun `git diff --check` passes.

### Integrity and Remaining Commit Action

`PROJECT_GOAL.md`, `AGENTS.md`, both acceptance authorities, the hidden
protocol, acceptance manifest, immutable lock, package/lock/verifier, and all
protected configuration remain outside the diff. The four lock-governed files
match their baseline and active hashes; CAL-1 remains `open_not_started`.
State/runtime/shipped schema are `1.11.0`. The untracked human plan remains
exactly
`53ea98fb1cb880163a02d3b1d9365963e3fe891025ae3630f00bd4c9232293b1`.
`git diff --check` passes before this documentation update. HEAD remains
Session 1 commit `069527e92744149f0041dcaa3c978cf773adace9`; origin/master remains its
parent `96c3eb2da170da5eed4cf99dc0becf5eb256d138`.

Every candidate-prepare behavioral acceptance criterion has now been observed,
including exact pre-launch replay, three ambiguous preserved windows, repeated
resume convergence, synchronized lease/CAS exclusion, complete adversarial
classification, authorized checkpoint adoption, strict runtime/shipped schema
and migration fencing, all four earlier-operation compatibility, read-only
status/Doctor/inspection, and zero-skip broad regressions. This closes no
product, readiness, human-acceptance, hidden-validation, CAL-1, or WP6 gate.

**Closure.** The cohesive local commit containing this record closes
`candidate-prepare` and the previously omitted WP2 recovery boundary. No
Session 2 acceptance criterion remains unresolved. The only working-tree entry
permitted after final verification is the protected untracked human improvement
plan at its recorded hash. No push occurred. WP6, CAL-1, hidden validation,
product/readiness implementation, product completion, autonomous readiness,
and human acceptance remain outside this completed plan and were not started.
