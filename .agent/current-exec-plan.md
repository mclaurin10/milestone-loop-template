# Current Execution Plan

Status: C6d-R2 implementation verified; clean post-commit and exact hosted observations remain. Updated: 2026-09-06 UTC. Owner: authorized WP6e continuation.

## Objective

Repair the actual Linux hosted failure removing a disposable source-transition Git fixture. The immediate consumer is the unchanged complete source-authority-transition acceptance file. Pause C6e expansion while resolving this harness regression, then resume the full source migration/candidate/provider/mutation obligations.

## Goal Constraints

Preserve every test body, identity, assertion and original deadline, frozen authorities, protected public argv/workflow, source state absence and historical WP6e BLOCKED disposition. Do not suppress persistent cleanup errors or delete outside the exact owned temporary directory. No shared Git configuration, service or package changes. No source activation or readiness/performance claim.

## Baseline Evidence

At bb312f3aa9c3708aa7e0d11d3cd682b96a4f5efa (tree f2e7f228e215e1d1680fae4b933df4c20f27deee), actual Linux job 101513230820 in run 34043089595 reported 949/950 controller cases passing. The sole failure is source-authority-transition.test.ts, fullName "exact source authority transition projection refuses an existing epoch record even with a self-authored PASS": ENOTEMPTY removing /tmp/source-transition-test-6SFivJ/.git/objects/info. It occurred at 751ms and did not reach a test timeout. The original package-runtime test passed. No controller PASS receipt or later root-unit execution exists for this Linux job.

Three other jobs passed; Windows controller 101513230702 remains in progress. Four available provider ZIPs were hash-checked/safely extracted under artifacts/wp6e-source-publication-20260906/hosted-bb312f3 in the primary workspace; the Linux ZIP hash is 79512c713d7f4d0d1d6562e70600e5fa4e628c592a01b6f2d022c7e4badf4063. Preserve the full cohort before a successor same-ref push.

The fixture uses exact mkdtemp roots, real shared Git clones/commits and a single default rm(...recursive:true,force:true) in afterEach. No captured directory inventory/process trace proves which writer caused the nonempty directory. Do not assert a proven Git maintenance cause. The production source projection awaits its synchronous Git commands. Node 24.18.0 documents bounded rm retries for ENOTEMPTY, and existing repository fixture teardown already uses maxRetries. Git documents disabling automatic maintenance through local configuration.

## Steps

1. Preserve the hosted raw failure and original test bytes, and run the unchanged complete transition/runtime files natively to establish whether the failure reproduces locally. A passing local run does not relabel the hosted failure.
2. Confine automatic Git housekeeping in disposable fixture clones using clone-local configuration, and use bounded native filesystem retries for teardown with the same original test deadlines. Validate exact owned absolute paths before recursive removal. Keep persistent errors fatal.
3. Compare every original test body/identity/deadline as syntax trees; run full affected files, typecheck/lint/format, source architecture/dependency checks and five invariants. Preserve failures, then audit and commit a narrow verified change.
4. Reproduce the retained audit, focused checks, dependencies and actual consumed production build from a clean repair commit. Collect all five exact hosted jobs before claiming cross-platform repair. Restore C6e exactly and continue through the actual committed source candidate/four partitions/65-minute provider boundary and committed 5(a)/5(b).

## Acceptance Criteria

The same real source-transition tests execute with their original assertions and deadlines. Teardown stays within its own real temporary directory, tolerates only bounded documented transient filesystem failures, and still reports persistent failure. No source-state/ref initialization or public contract changes. Independent evidence identifies the actual failed hosted commit and the actual repaired commit separately.

## Verification

Pinned Node 24.18.0/pnpm 11.15.1; use the existing source-scope-focused receipt runner, complete affected files, original invariant suite and source static checks. The consumed build's clean-tree admission requires post-commit execution; never fake a commit identity. Retained evidence and supporting post-commit outputs go to owned ignored artifact directories, with sealed provenance/audit scripts in docs. Native Windows, Linux, Server 2022 and qualified OCI claims remain distinct.

## Risks and Recovery

The underlying transient writer was not observed. Local automatic-maintenance suppression is a precaution in newly owned clones, not an assertion about the hosted cause. Bounded rm retries do not forgive a persistent failure or extend a test deadline. Preserve all old stashes, the untracked roadmap and the unfinished C6e implementation in the primary workspace.

## Progress and Evidence

Unchanged native baseline passed all 32 original transition/runtime cases at artifacts/transition-cleanup-regression/baseline-focused-1 (receipt 5b82254214621040da5a3922b97670d6f32a5b311472edfec0cd2d491491f6da). The intermittent hosted failure did not reproduce locally. Implemented clone-local automatic-housekeeping suppression, canonical exact owned-path checks and five native retries with 25ms linear backoff. Original test suites and deadlines are unchanged; independent syntax-tree comparison is in the retained auditor. The first repair failed two original CLI-output teardowns (30 PASS/2 FAIL) because their UUID-named roots are below controller artifacts, not the system temporary parent. Retain that raw failure and no PASS receipt. Correct only teardown to recognize the two original CLI prefixes under the exact canonical artifacts parent, then rerun complete files. No root outside the recorded owned locations is deleted. Typecheck/lint/format/architecture passed; dependencies and all five invariants also passed (84754ms; no performance interpretation). Final corrected focused run passed all 32 cases (3228b6c9ccf1e30fc3f7858ab86b7fed6ac5ca229af3b830ab0e1135c0fc635a); affected static reruns passed. Fresh retained audit validates 409 files / 3908229 bytes / 49 receipts and complete unchanged suite syntax trees (87c444475a3c25fbde0965eec1d37231453aabfff330ed4e2c6899c5811eef85). Seal ZIP is 1816614 bytes / SHA256 7a7f61733e455c405e9ebb193efa625b7870ab4a1024d4b6a116eacf2e6d4f6e. Preserve all failed observations. The clean post-commit collection destination is artifacts/wp6e-source-publication-20260906/transition-cleanup-postcommit in the primary workspace. No source activation or controller-state initialization.

Owned remote-free repair clone .tools/wp6e-transition-cleanup-fix starts at exact bb312f3 with a clean tree; frozen offline copy installation reused 138 packages. No state or private controller refs initialized. The new hosted cohort is incomplete; do not cancel the running Windows job with a same-ref push.

## Next Action

Commit the verified narrow repair, run the retained audit, complete focused selection, dependencies and consumed build from a fresh clean clone, and independently retain exact commit/tree and receipt observations. Preserve the in-flight bb312f3 Windows cohort before a successor push; restore C6e and continue the full task.
